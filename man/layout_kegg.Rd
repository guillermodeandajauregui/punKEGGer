% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layout.R
\name{layout_kegg}
\alias{layout_kegg}
\title{Extract node layout coordinates from a KEGG KGML file}
\usage{
layout_kegg(kgml)
}
\arguments{
\item{kgml}{A parsed XML document from \code{xml2::read_xml()}.}
}
\value{
A tibble with columns: \code{id}, \code{x}, \code{y} (corresponding to KEGG entry IDs).
}
\description{
Parses the \code{graphics} element of each node in the KGML to extract manual layout
positions (x, y), which are typically defined for KEGG maps.
}
\examples{
kgml_file <- system.file("extdata", "hsa04210.xml", package = "punKEGGer")
doc <- xml2::read_xml(kgml_file)
layout_tbl <- layout_kegg(doc)
head(layout_tbl)
}
