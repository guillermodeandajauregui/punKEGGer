% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nodes.R
\name{extract_kegg_nodes}
\alias{extract_kegg_nodes}
\title{Extract KEGG node information from a KGML file}
\usage{
extract_kegg_nodes(kgml)
}
\arguments{
\item{kgml}{A parsed XML document from \code{xml2::read_xml()}.}
}
\value{
A tibble with columns: \code{id}, \code{meta_id}, \code{type}, \code{kegg_id}
}
\description{
Parses all \code{entry} elements from a KEGG KGML XML document and extracts ID, name,
type, and individual KEGG IDs. Supports multi-gene entries (e.g., \code{hsa:1234+hsa:5678})
by expanding them into separate rows with a shared \code{meta_id}.
}
\examples{
kgml_file <- system.file("extdata", "hsa04210.xml", package = "punKEGGer")
doc <- xml2::read_xml(kgml_file)
nodes <- extract_kegg_nodes(doc)
head(nodes)
}
